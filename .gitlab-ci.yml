stages:
  - build

build_job:
    stage: build
    script: 
      echo "Building..."
      docker build -t leaguele-app .
      docker save leaguele-app > image.tar
    artifacts:
      paths:
        - image.tar
      expire_in: 12 minutes